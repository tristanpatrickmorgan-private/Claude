name: Weekly Review Email

on:
  schedule:
    - cron: '23 5 * * 0'  # 07:23 Sunday Swedish time (CEST, UTC+2) — off-hour to dodge GHA cron queue
  workflow_dispatch:      # allows manual trigger from GitHub for testing

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  send-weekly-email:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Set date
        run: echo "TODAY=$(TZ='Europe/Stockholm' date +'%a %d %b %Y')" >> $GITHUB_ENV

      - name: Generate email content
        run: TZ='Europe/Stockholm' python scripts/weekly_review.py > email_body.txt

      - name: Send email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: ${{ secrets.GMAIL_USERNAME }}
          password: ${{ secrets.GMAIL_APP_PASSWORD }}
          subject: "Weekly review — ${{ env.TODAY }}"
          to: tristanpatrickmorgan@gmail.com
          from: "Personal Assistant <tristanpatrickmorgan@gmail.com>"
          body: file://email_body.txt
